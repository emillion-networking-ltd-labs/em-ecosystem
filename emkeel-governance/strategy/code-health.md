# Strategy: code-health

Status: APPROVED   <!-- operador, human gate = merge de #608 (aprobación original) + consolidación ECO-224 + saneamiento ECO-227 (reconcilia fundamento con decisiones) -->
Strategy: code-health   <!-- feature specs reference this with a `Strategy: code-health` line -->
Impact: high   <!-- topología de enforcement SELF-CONTAINED en em-ecosystem (hook local + gate de emkeel); emkeel shipea el motor del gate (check_code_health), este repo declara los checks en code-health.toml. -->

## Goal
Decidir CÓMO la flota impone "código sano" de forma sistemática al MODIFICAR ficheros en un ticket — qué es gate-duro (mecánico) vs review-de-juicio, DÓNDE vive cada control (hook local + gate CI de emkeel, **AMBOS en em-ecosystem** — self-contained), y el modelo ratchet para no hacer big-bang sobre la deuda legacy.

## Context
<!-- grounded facts ONLY — cite file:line (repo) or a URL (market) for every claim -->
- **De dónde sale:** revisando `design-system/tokens/tokens.css` para un barrido de idioma aparecieron problemas que el idioma no cubre — duplicación (`.light` re-escribe casi todo `:root`; feedback declarado 4-5 veces con líneas muertas — p.ej. `.dark` fija `--color-error` en channels y lo pisa con `rgb()` en el mismo bloque, `tokens.css:493` pisado por `tokens.css:543`), incoherencia de patrón (surfaces usan indirección de dos nombres, feedback usa auto-referencia `--color-warning: var(--color-warning)`, `tokens.css:277`), y comentarios-prosa largos casi todos en español.
- **ESLint hoy:** flat config con presets Next+TS (`nexacore-dashboard/eslint.config.mjs:4-9`); tres reglas `react-hooks` v6 apagadas por 126 falsos positivos (`nexacore-dashboard/eslint.config.mjs:20-46`, SCRUM-377) — el caso canónico de "gate de juicio que se apaga". El API es más estricto (`recommendedTypeChecked` + security + `prettier:error`, `nexacore-api/eslint.config.mjs:12-34`). **NO hay** reglas de complejidad/max-lines/naming/no-dup/dead-code en ningún config (confirmado). Endurecer pendiente en SCRUM-372 (`nexacore-dashboard/eslint.config.mjs:3`).
- **CI ya muerde los `warn`:** Security Layer 3 corre `eslint … --max-warnings 0` (`.github/workflows/security.yml:151-229`) → los warnings ESLint bloquean en CI aunque no en local.
- **Duplicación:** `jscpd` con `threshold:100` = nunca auto-falla (`.jscpd.json:3-5`); el bloqueo lo hace el hook local leyendo "Found N clones" (`.husky/pre-commit:40-56`) SOLO sobre `.ts/.tsx` staged de producción (`.husky/pre-commit:34`). En CI es advisory.
- **Sin stylelint / sin knip:** confirmado 0 linters de CSS y 0 detectores de dead-code en el repo.
- **Patrón RATCHET ya existe:** en el design-system, `_ratchet.mjs:1-28` — baseline que solo decrece, auto-promociona a `enforce` al llegar a 0, "espejo de ESLint bulk-suppressions"; baseline vivo en `enforcement/ratchet-baseline.json` (`comment-language:1060`, `typography:38`, `html-primitives:24`, `attenuation:0`). **emkeel ya provee la mitad de ENFORCEMENT del ratchet** vía el gate `check_code_health` (compara el conteo de cada check contra su baseline en `code-health.toml`); lo que aún NO existe es el mecanismo que RECOMPUTA el baseline desde `main` MERGEADO (pieza del runbook). El helper `emkeel/src/emkeel/review.py:2-3` es advisory (IA-review), no computa baseline.
- **Precedente "salud horneada en ciclo de vida por-ítem":** el DoD del DS (`DEFINITION-OF-DONE.md:9-38`) + el gate `check-piece-complete.mjs:76-130` (evidencia máquina A/B/F/Q/C/O + pasos `[H]` humanos) — WIP `Migration/` exento, `Primitives/` certificada, promoción bloqueada si falta evidencia.
- **Mecanismo por-diff en el ecosistema:** `check_maint_scope.changed_files(base)` = `git diff --name-only origin/{base}...HEAD` (`emkeel/src/emkeel/gates/check_maint_scope.py:29-38`, base de `EMKEEL_BASE_REF`), reutilizado por `check_critical_integration` y `check_strategy_process`. **El gate de code-health YA existe:** `check_code_health` (emkeel ≥0.1.111, cableado en `emkeel-ci`) — el primero de su clase (el resto de gates emkeel son de PROCESO); está DORMIDO hasta que `code-health.toml` declare checks en la raíz.
- **No-bypasseabilidad ya resuelta:** gates server-side + branch protection require `gates` + `doctor` verifica que estén realmente enforced (`emkeel/AGENTS.md:4-5,33`; `emkeel/src/emkeel/doctor.py:137-139`). Un gate nuevo la hereda con un step en `ci.yml`.
- **Estándares ya citados en el repo:** CISQ ASCMM-MNT-19 + ISO 25010 (`.husky/pre-commit:6-8`, `security.yml:267`, `.husky/pre-push:12`).
- **Mercado — ratchet "solo lo que tocas" es first-party desde 2025:** ESLint bulk-suppressions (`eslint --suppress-all` → `eslint-suppressions.json`, nuevo código enforced, legacy no) https://eslint.org/blog/2025/04/introducing-bulk-suppressions/ ; para checks arbitrarios, **betterer** (`.betterer.results` = baseline, falla si empeora) https://github.com/phenomnomnominal/betterer
- **Mercado — el catálogo mecánico de mantenibilidad es finito y automatizable:** CISQ **ASCMM = mantenibilidad = sub-slice CWE-1130** (~29 debilidades), dentro del superset CWE-1128 (77 debilidades / 4 categorías). Debilidades mecánicas mapeadas a CWE: Dead Code CWE-561, Complejidad CWE-1121 (umbral 20), Redundancia CWE-1041, Ficheros grandes CWE-1080, **Dependencias circulares CWE-1047** https://cwe.mitre.org/data/slices/1130.html ; ISO 25010 mantenibilidad = modularidad/reusabilidad/analizabilidad/modificabilidad/testabilidad https://www.perforce.com/blog/qac/what-is-iso-25010
- **Hueco de la propia estrategia (crítica de completitud):** el ACOPLAMIENTO / dirección de dependencias (ciclos CWE-1047, fronteras entre módulos NestJS controller→service→DTO, corte server/client de Next `'use client'`) es un invariante que el repo ya declara pero NADA verifica; es mecánico y gate-able por-diff con `eslint-plugin-boundaries` (reusa el toolchain ESLint) o `dependency-cruiser` — es la única clase de defecto ESTRUCTURAL/inter-módulo del catálogo, no intra-fichero.
- **Mercado — consenso fuerte: gatear lo mecánico, enrutar el juicio a review.** Reglas cuyo fix es "reestructura tu código" pertenecen a review, no a un gate rojo/verde; el `warn` que nadie atiende es un anti-patrón https://dev.to/thawkin3/eslint-warnings-are-an-anti-pattern-33np ; el caso `exhaustive-deps` (fix = decisión de diseño; los devs lo desactivan en bloque; petición de ignores finos cerrada sin resolver) https://react.dev/reference/eslint-plugin-react-hooks/lints/exhaustive-deps
- **Mercado — dead-code:** `knip` es el estándar 2025 para monorepos (ts-prune archivado 2025-09-19, apunta a knip) https://github.com/nadeesha/ts-prune ; https://knip.dev/explanations/comparison-and-migration
- **Mercado — AI review:** complementa, nunca reemplaza; el ruido/falsos-positivos es el modo de fallo central (~40% de alertas ignoradas) https://www.cubic.dev/blog/the-false-positive-problem-why-most-ai-code-reviewers-fail-and-how-cubic-solved-it ; Claude Code Action es el más "promptable" (defines tu rúbrica) https://github.com/anthropics/claude-code-action

## Options
<!-- at least 2 real options; EVERY row MUST cite a Source (file:line or URL). -->
| # | Option | Source | Pros | Cons | Risk |
|---|--------|--------|------|------|------|
| 1 | **Capas + ratchet nativo (extender lo que hay).** Endurecer ESLint (complexity/max-lines/sonarjs) + añadir stylelint (CSS/tokens) + knip (dead-code) + jscpd-como-gate; TODO ratcheteado por-diff (ESLint bulk-suppressions, "solo lo nuevo"); cableado como **gate required de emkeel** (`check_code_health`, backstop no-bypasseable) + hook local (feedback proactivo rápido). Self-contained en em-ecosystem. Juicio = 1 revisor IA advisory (Claude Code Action), nunca gate duro. | https://eslint.org/blog/2025/04/introducing-bulk-suppressions/ · `_ratchet.mjs:1-28` · `check_maint_scope.py:29-38` · https://cwe.mitre.org/data/slices/1130.html | Reusa infra existente (ratchet del DS, diff-helper y no-bypass de emkeel); estándar de industria; per-diff first-party = sin big-bang; cada capa para lo suyo | Más piezas que mantener (ESLint+stylelint+knip+jscpd+AI); config inicial no trivial; el ratchet del DS es JS, generalizarlo a la flota es construcción | Fatiga de reglas si se sobre-gatea el juicio (kill-criterion #1); coste de mantenimiento (#2) |
| 2 | **Plataforma SonarQube.** Adoptar Sonar como motor único de mantenibilidad: quality gate sobre CÓDIGO NUEVO (duplicación ≤3%, maintainability rating, complejidad) computado per-diff nativamente; bloquea el PR. | https://docs.sonarsource.com/sonarqube-server/2025.1/user-guide/code-metrics/metrics-definition/ | Un solo motor, per-new-code nativo (encaje per-diff ideal), letter-grade, SARIF a GitHub, cubre el catálogo CISQ de golpe | Peso de infra (servidor/SaaS, coste); "otro portero" fuera de emkeel (rompe "los gates viven en emkeel"); ignora dup/coverage hasta +20 líneas nuevas | Dependencia de plataforma externa; el equipo la percibe como caja negra ajena a la gobernanza emkeel |
| 3 | **Consolidar en Biome v2.** Reemplazar ESLint/Prettier por Biome (rápido, type-aware subset, reglas cross-file `noImportCycles`/`noUnusedClasses`), manteniendo `tsc` para el chequeo real. | https://biomejs.dev/blog/biome-v2/ · https://biomejs.dev/blog/biome-v2-5/ · https://betterstack.com/community/guides/scaling-nodejs/biome-eslint/ | Muchísimo más rápido (per-diff barato); formatter+linter+dead-code en una herramienta; cross-file nativo | Pierde reglas que necesitas (naming-convention, react-hooks, ecosistema 1000+ plugins); type-aware solo ~85% de typescript-eslint; migración de todo el toolchain | "ESLint sigue siendo lo seguro para bases establecidas"; regresión de cobertura de reglas durante la migración |
| 4 | **Generalizar el patrón del design-system (gates a medida).** Construir `check-*.mjs` propios por dimensión de mantenibilidad (complejidad/dup/dead-code/tamaño) + `_ratchet` + `protected-dimensions`, como ya se hizo para tokens — sin plataforma ni linters de terceros. | `check-piece-complete.mjs:76-130` · `_ratchet.mjs:1-28` · `enforcement/protected-dimensions.json` | Coherente con lo que la flota ya domina; control total; reusa el meta-gate + ratchet ya probados; cero dependencia externa | Reinventa lo que ESLint/knip/jscpd ya hacen mejor; coste de construir y mantener analizadores propios; riesgo de peor calidad que el estándar | Falsa economía (#2): mantener parsers a mano cuesta más que el defecto que previene |

## Recommendation
<which option + why — this is judgment; the human approves it at the gate>

**Síntesis con espina en la Opción 1, tomando el ratchet+registro de la Opción 4, descartando 3, y RESOLVIENDO 2 (Sonar) = NO (decisión de coste registrada abajo en Decisions).**

**Decisiones registradas** — cada `Dn` tiene su pieza en el runbook (`code-health.runbook.md`); el gate verifica la trazabilidad en AMBAS direcciones (toda decisión tiene constructor, toda cita existe):

- **D1 — Idioma INGLÉS en código, gateado.** Código + comentarios + JSDoc/docstrings en `.tsx/.ts/.mjs/.css` = inglés; español SOLO en conversación + gobernanza (ADRs/specs/records/**estrategias**) + puntero/memoria. Se impone con `check-comment-language` (ya existe, ratchet baseline 1060), hoy **solo en el design-system** → se **generaliza a toda la flota** (nexacore-api/dashboard/satélites). La frontera vive en el SCOPE del gate: escanea CÓDIGO, NO `emkeel-governance/` (por eso este doc va en español, correcto). Bakeado en el proceso = no depende de que alguien se acuerde.
- **D2 — ESLint endurecido** (complexity / max-lines / `eslint-plugin-sonarjs`) para JS/TS. Herramienta ESTÁNDAR, no reinventada: reinventarla (Opción 4) es la falsa economía que activa el kill-criterion #2. Ataca debilidades mecánicas de CISQ ASCMM https://cwe.mitre.org/data/slices/1130.html.
- **D3 — stylelint** para CSS/tokens (hueco actual confirmado: 0 linters de CSS en el repo). Caza dead custom-properties intra-bloque.
- **D4 — knip** para dead-code (estándar 2025 en monorepos; ts-prune archivado) https://knip.dev/explanations/comparison-and-migration.
- **D5 — `eslint-plugin-boundaries` / `dependency-cruiser`** para fronteras entre módulos + ciclos (CWE-1047): la única clase de defecto ESTRUCTURAL inter-módulo del catálogo, que el repo ya declara como invariante pero nada verifica.
- **D6 — Ratchet "solo lo que tocas".** El baseline solo DECRECE y se **recomputa desde `main` MERGEADO** (doctrina `_ratchet.mjs:1-28`), nunca lo escribe un actor anticipado. El ratchet completo son **DOS mitades**: **`check_code_health` (enforcement — compara el conteo del check contra el baseline COMMITTED en `code-health.toml`; NO recomputa) + la Pieza 9 (recompute del baseline desde `main` mergeado)** → juntas **superan a `betterer`** (que hacía AMBAS: enforce + mantener el baseline), que ya no hace falta. Las ESLint bulk-suppressions siguen siendo útiles DENTRO de ESLint https://eslint.org/blog/2025/04/introducing-bulk-suppressions/.
- **D7 — Topología en 2 puntos, SELF-CONTAINED en em-ecosystem:** hook local (feedback proactivo) → **gate CI de emkeel** (`check_code_health`, backstop no-bypasseable, `doctor.py:137-139`). El gate YA existe (emkeel ≥0.1.111, cableado en `emkeel-ci`); su config es `code-health.toml` en la RAÍZ (agent-editable; `emkeel update` no lo regenera ni el guard lo bloquea, a diferencia de `emkeel.toml`). El hook cubre el rol proactivo por-diff; el gate es el backstop no-saltable.
- **D8 — Duplicación = ADVISORY, nunca gate duro.** Es relacional (exige dos sitios) y no mapea al ratchet "solo lo nuevo": un PR que toca a la vez auth V1 y V2 resurface el clon sin que ninguna cara sea "nueva" → gatearlo es el perfil exacto de SCRUM-377. Va a **report + IA-review**, con **exclusiones declaradas** de coexistencia intencional (como `.jscpd.json:7-25` ya exime las copias em-ui).
- **D9 — Juicio = revisor advisory `eco-reviewer`, NUNCA gate duro.** Organización/abstracción/coherencia → review, no rojo/verde: gatearlo reproduce `exhaustive-deps`/SCRUM-377 (kill-criterion #1). Independiente ("sin juicio previo" = sin apego de autor; varios + mayoría), PERO con rúbrica + convenciones/ADRs + **fichero completo** — juzgar el diff aislado es el fallo raíz de los revisores IA https://www.cubic.dev/blog/the-false-positive-problem-why-most-ai-code-reviewers-fail-and-how-cubic-solved-it. Corre pre-merge, advisory.
- **D10 — Drenaje proactivo = skill `/code-health <ámbito>` que AUDITA y PROPONE un diff.** Complementa al ratchet reactivo (que nunca drena un fichero frío). No commitea ni tickeza: entra por el flujo gobernado (`emkeel start` → PR → gates), un ticket por tanda.
- **D11 — Métrica de eficacia** (para poder MEDIR los kill-criteria, hoy solo enunciados): aceptación de la IA-review · falsos-positivos por gate · **convergencia del ratchet** (los baselines DEBEN bajar; si `comment-language`=1060 no baja, el ratchet no funciona = kill-criterion #4).

**Refinamientos del panel adversarial (5 lentes) — correcciones honestas a la recomendación:**
1. **jscpd NO es gate duro — es advisory.** La duplicación es RELACIONAL (exige dos sitios), así que el ratchet "solo lo nuevo" no mapea: un PR que toca a la vez auth V1 y V2 (coexistencia intencional, la mayoría de los 85 clones del rename) resurface el clon sin que ninguna cara sea "nueva". Gatearlo en duro = perfil exacto SCRUM-377 → primer gate que la flota apagaría. **Dup va a la capa advisory (report + IA-review)**, con exclusión declarada de coexistencia intencional (como `.jscpd.json:7-25` ya exime las copias em-ui).
2. **El gate CSS caza DEAD custom-properties, no la redundancia estructural.** stylelint `declaration-block-no-duplicate-custom-properties` caza las líneas muertas intra-bloque (`tokens.css:493` pisada por `:543`) — victoria limpia. Pero la duplicación TITULAR del caso (`.light`≈`:root`, feedback 5× cross-block) es cross-selector = **review/juicio, no gate** (y `.light` es intencional). No sobre-prometer que "el gate cierra la duplicación".
3. **Coste real / kill-criterion #2 — RESUELTO (2026-07-16): Sonar = NO.** La Opción 1 son ~6 piezas a mantener + generalizar el ratchet a la flota (el **gate emkeel YA lo construyó /emkeel-engineer** — `check_code_health` ≥0.1.111) + coste RECURRENTE de la capa IA (Claude Code Action = tokens/API por PR; no es "advisory gratis") + setup de baselines flota-wide (recuerda: `comment-language` ya arrastra 1060). La Opción 2 (Sonar) es UN motor pero su PR-analysis exige Developer Edition **de pago** (la Community no lo trae; SaaS Team desde ~$34/mes). Descarté Sonar por gobernanza ("portero fuera de emkeel"), NO por coste — y el kill-criterion #2 exige la comparación económica: **¿mantener las piezas propias cuesta menos que una plataforma de pago?** **RESUELTO: el operador eligió el stack in-house (Sonar = NO)** — y `eslint-plugin-sonarjs` da la paridad de reglas de mantenibilidad gratis, así que no se pierde cobertura. Ver Decisions.
4. **Cita CISQ corregida** — ASCMM = mantenibilidad = CWE-1130 (no el superset 1128).

**Métrica de eficacia (para poder medir los kill-criteria, hoy solo enunciados):** tasa de aceptación de la IA-review (comentarios accionados / emitidos; objetivo evitar el ~40% ignorado), tasa de falsos-positivos de cada gate mecánico, y convergencia del ratchet (los baselines DEBEN bajar trimestre a trimestre; si `comment-language`=1060 no baja, el ratchet no funciona = kill-criterion #4). Sin esto no se puede juzgar si el sistema previene defectos o solo mete ruido.

**Drenaje proactivo (complemento al ratchet reactivo).** El ratchet solo drena al TOCAR un fichero → lo frío (p.ej. `comment-language:1060`) no baja solo, y la métrica de eficacia exige que baje. Se añade un **asistente de campaña** (skill `/code-health <ámbito>`) que **AUDITA y PROPONE un diff** sobre un ámbito elegido — NO una máquina que auto-arregla:
- **Propone, no arregla en ciego** (regla del operador *eco-agree-before-acting*). El auto-fix solo sería admisible para el subconjunto **determinista y gate-duro** (comentario sin directiva, custom-properties duplicadas intra-bloque vía `declaration-block-no-duplicate-custom-properties`); dedup/dead-code/`.light`≈`:root` van a review porque la estrategia YA los mandó ahí (relacional/intencional). Modos de fallo reales: knip no ve usos DINÁMICOS ni exports consumidos por el toolchain; un comentario español puede ser una directiva load-bearing (`eslint-disable`, `@ts-expect-error`, `@ds-role`, marcadores `[H]` que lee `check-piece-complete.mjs`); editar a mano un `@em-ui-adapted` = `check-fleet-report` rojo (reconciliar con `em-ui update`, no hand-edit).
- **El baseline se recomputa desde `main` MERGEADO — nunca lo escribe el skill.** El baseline refleja la deuda REAL en `main` (`_ratchet.mjs:1-28`); bajarlo antes del merge = baseline fantasma → deuda>baseline imposible de satisfacer, o falso 0 → auto-`enforce` roto. El fix baja el baseline como EFECTO del merge (el ratchet recomputa), no como acto anticipado.
- **No commitea ni tickeza:** la campaña entra por el flujo gobernado normal (`emkeel start` → branch → PR → gates), un ticket por tanda, pasando los MISMOS gates (incluido `Strategy: code-health` + `## Alignment`). El precedente `/audit` (ECO-1/10-audit) son REGISTROS de lectura — no autorizan mutar producción.

**Sonar = NO** — decisión de coste registrada en Decisions. Y **el motor del gate YA está construido en emkeel** (`check_code_health`, ≥0.1.111): lo que resta es declarar los checks en `code-health.toml` y construir las piezas del runbook — no un motor.

## Non-goals
- NO gatear en duro el JUICIO de diseño (abstracción, "¿es la abstracción correcta?", coherencia, largo de comentarios) — va a review advisory (evidencia: https://react.dev/reference/eslint-plugin-react-hooks/lints/exhaustive-deps).
- NO big-bang de la deuda legacy — solo ratchet (la deuda decrece al TOCAR el fichero; `_ratchet.mjs:1-7`).
- NO fijar aquí los umbrales exactos (complexity=N, max-lines=M, dup=%) — eso es ejecución/ADR.
- NO fijar aquí la FORMA concreta del revisor de juicio (subagente propio vs herramienta de tercero como Claude Code Action/CodeRabbit; ambas advisory y combinables) ni el ALCANCE del auto-fix del drenaje (si además de proponer llega a auto-arreglar el subconjunto determinista) — se eligen al construir sus piezas (D9, D10), con casa en el runbook. Lo arquitectónico ya está decidido: el juicio va a review advisory y el drenaje PROPONE.
- NO fijar aquí el ORDEN de entrada de las dimensiones — se decide pieza a pieza en el runbook.
- NO adoptar una plataforma pesada (Sonar) salvo que el operador lo elija; NO migrar el toolchain a Biome ahora.
- NO reemplazar el review humano — la capa IA lo complementa (https://www.cubic.dev/blog/the-false-positive-problem-why-most-ai-code-reviewers-fail-and-how-cubic-solved-it).
- NO gatear la DUPLICACIÓN en duro — es relacional (dos sitios) y no mapea al ratchet "solo lo nuevo"; va a la capa advisory + IA-review, con exclusión de coexistencia intencional (V1/V2). Las FRONTERAS/ciclos sí son gate duro (son inter-módulo pero decidibles y con fix local).
- NO gatear el IDIOMA de la GOBERNANZA (ADRs/specs/records/estrategias) ni del puntero/memoria — ahí el español es legítimo; el gate de idioma escanea SOLO código.
- NO abarca el RENDIMIENTO (performance efficiency de ISO 25010) — "código sano" aquí = MANTENIBILIDAD (el catálogo CISQ ASCMM). Perf es otra característica de calidad, con su propia herramienta/estrategia; frontera de alcance consciente. Seguridad y tests tampoco se re-cubren: ya tienen sus gates (Security Pipeline `security.yml`; gates de tests).

## Decisions
**ADR: [ADR-034](emkeel-governance/adr/034-code-health-enforcement.md)** (`accepted`, ECO-212) — registra esta estrategia. **Runbook de implementación: [code-health.runbook.md](emkeel-governance/strategy/code-health.runbook.md)** (el norte de ejecución, pieza a pieza con `done_when`).

**Saneamiento (ECO-227, 2026-08-02):** reconciliada la capa de fundamento (Context/Opciones) con las decisiones; la topología se describe en positivo (hook local + `check_code_health`, self-contained). La decisión no cambia.

**RESUELTAS (refinamiento ECO-224, 2026-07-16):**
- **Aprobación:** estrategia APROBADA (human gate = merge de #608; consolidada aquí). `Status: APPROVED`.
- **Topología = 2 puntos, SELF-CONTAINED en em-ecosystem:** hook local (proactivo) → gate `check_code_health` de emkeel (backstop no-saltable). El motor del gate YA está en emkeel (≥0.1.111, cableado en `emkeel-ci`); la config vive en `code-health.toml` (raíz del repo, agent-editable). El hook cubre el rol proactivo por-diff.
- **Sonar = NO** — code-health = stack libre in-house. Motivos: el análisis de PR de SonarQube es Developer Edition **de pago** ("contact sales"); SonarCloud es gratis SOLO en repos públicos y estos son PRIVADOS (privado → de pago); SonarQube Community self-hosted es gratis+privado pero **NO** hace análisis de PR; el truco "repo público espejo" se descarta (expondría el código privado). **Clave:** `eslint-plugin-sonarjs` v2+ (LGPL-3.0, gratis) ya expone TODAS las reglas de calidad de SonarJS a ESLint → paridad de mantenibilidad esencialmente completa y gratis, dentro de la gobernanza. Lo único de pago inaccesible = SAST/taint/dataflow = SEGURIDAD (fuera de alcance; ya cubierto por el Security Pipeline). Se renuncia solo al dashboard visual/tendencias (nice-to-have, recuperable con Community nightly). Cierra el kill-criterion #2 a favor del stack in-house.
- **Duplicación = ADVISORY**, nunca gate duro (relacional; no mapea al ratchet "solo lo nuevo"; gatearla bloquearía coexistencia intencional V1/V2).

**DIFERIDAS a la ejecución** (las fija el operador ticket a ticket; ver runbook):
- **Qué dimensiones entran primero + sus umbrales** (complexity=N, max-lines=M, dup=%).
- **Auto-fix del drenaje:** si el skill `/code-health` auto-arregla el subconjunto DETERMINISTA (comentario sin directiva, dead-props intra-bloque) o **solo PROPONE** — muta producción, riesgo propio, decisión explícita.
- **eco-reviewer:** (a) subagente propio (hermano de `eco-verifier`, con rúbrica+convenciones) vs (b) tercero (Claude Code Action / CodeRabbit); ambas advisory, combinables.
