# Emkeel config (non-secret). Real credentials go in .env / GitHub Secrets.
[jira]
base_url = "https://emillionnetworking-ltd-labs.atlassian.net"
project_key = "ECO"

[github]
repo = "emillion-networking-ltd-labs/em-ecosystem"
runner = "self-hosted"   # Actions runner for emkeel-ci/jira-transition (ubuntu-latest | self-hosted | <label>)
# required_checks: status-check contexts that MUST be required on the default branch.
# `emkeel doctor` verifies they're enforced (catches "the check exists but isn't required").
# `gates` is always checked. Add your other CI gates here, e.g.:
# required_checks = ["gates", "Security Gate (All Checks)"]

[emkeel]
source = "emkeel~=0.1.0"
generated_with = "0.1.107"   # the Emkeel version that wrote this wiring (emkeel doctor checks it)
