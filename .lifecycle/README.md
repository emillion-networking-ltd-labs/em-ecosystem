# .lifecycle — governance trail (em-ecosystem)

NexaCore's lifecycle artifacts (plans / records / verify / state, under `artifacts/`)
and architecture / product specs (under `specs/`), driven by the
**em-development-framework** engine. The engine lives in a separate repo;
point it here with:

    LIFECYCLE_ROOT=~/projects/em-ecosystem  python3 <framework>/forge/tools/state-machine.py ...

Layout established in Wave 8 (SCRUM-572 cross-repo migration + SCRUM-574 rename
to `.lifecycle/{artifacts,specs}/`).
