# dependencies
/node_modules
/.pnp
.pnp.js

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# local env files
.env*.local

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# Playwright (SCRUM-350)
/test-results/
/playwright-report/
/blob-report/
/playwright/.cache/
# ECO-36: preserved visual-run output (CI artifact, never committed)
/visual-test-results/
/visual-report/

# Playwright Windows-local VRT snapshots (SCRUM-396) — CI baselines are
# chromium-linux; the win32 copies that `npx playwright test` writes on
# Windows have a different platform suffix and must never be committed.
tests/e2e/visual.spec.ts-snapshots/*-chromium-win32.png
